package All_tests;

public class Without_main {
    static int x = 10;
    static int y = 15;
    static int a = x + y;

    static {
        System.out.println(a); // ✅ executes when class is loaded
        System.exit(0);        // optional, otherwise it will throw "main not found"
    }

}
