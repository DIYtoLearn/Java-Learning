package Array_Strings;
import java.util.Arrays;
public class Array_reoccur_element {
    public static void main(String[] args) {
        int[] arr = {1,2,3,4,5};
        boolean[] visited = new boolean[arr.length];
        System.err.println(Arrays.toString(visited));
    }
}
